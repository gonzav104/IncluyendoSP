# =============================================================================
# .env.example — Incluyendo SP (Frontend)
# Copiá este archivo a .env y completá los valores reales.
# NUNCA commitees .env (está en .gitignore).
# =============================================================================

# URL base del BFF (incluyendo-sp-api)
VITE_API_URL=http://localhost:3000